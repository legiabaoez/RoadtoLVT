#include <bits/stdc++.h>
#define int long long
using namespace std;

main()
{
    int n; cin >> n;
    int odd=0,even=0;
    vector<int> a(n+1);
    for(int i=1;i<=n;i++){
        cin >> a[i];
        if(a[i]%2==0)odd++;
        else even++;
    }
    cout<<min(odd,even);
    return 0;
}
